const btn=document.querySelector('.menu-btn'),nav=document.querySelector('.nav-links');if(btn&&nav)btn.addEventListener('click',()=>nav.classList.toggle('open'));const io=new IntersectionObserver(e=>e.forEach(x=>{if(x.isIntersecting){x.target.classList.add('in');io.unobserve(x.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));

let serviceFilter='all',subFilter='all';const cards=[...document.querySelectorAll('.filter-card')];function applyGalleryFilters(){cards.forEach(c=>{const okS=serviceFilter==='all'||c.dataset.service===serviceFilter;const okSub=subFilter==='all'||c.dataset.sub===subFilter;c.hidden=!(okS&&okSub);});}document.querySelectorAll('[data-filter-service]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-filter-service]').forEach(x=>x.classList.remove('active'));b.classList.add('active');serviceFilter=b.dataset.filterService;applyGalleryFilters();}));document.querySelectorAll('[data-filter-sub]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-filter-sub]').forEach(x=>x.classList.remove('active'));b.classList.add('active');subFilter=b.dataset.filterSub;applyGalleryFilters();}));
